export class Block {
    private name: string;
    private image: HTMLImageElement;

    constructor( name: string, image: HTMLImageElement ){
        this.name = name;
        this.image = image;
    }
}